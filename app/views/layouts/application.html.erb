<!DOCTYPE html>
<html>
  <head>
	<title>Finalsite</title>
	<%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <style>
    	body{
    		background-color: #0080ff
    	}



    </style>
  </head>
  
  <body>
	<!-- Navigation -->

	<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation" style="background-color: #0040ff; font-color: white;">
		<div class="container">
			<div class="navbar-header" style="color: white;">
				<%= link_to "Majo's Forum", root_url, class: "navbar-brand" , style: "color:white" %>
			</div>
	        <div  class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
				<ul class="nav navbar-nav">
					
					<%- if current_user %>
						
						<li>
							<%= link_to "Upload", new_post_path %>
						</li>
						<li>
							<%= link_to "Logout", destroy_user_session_path, method: :delete, data: {confirm: "Are you sure?"} %>
						</li>
						
					<% else %>

					<li>
						<%= link_to "Login", new_user_session_path ,style: "color:white"%>
					</li>
					<li>
						<%= link_to "Register", new_user_registration_path ,style: "color:white"%>
					</li>
					<% end %>
				</ul>
			</div>
		</div>
	</nav>
             
<!-- Page Content -->
    <div class="container">
      <div class="row">
        <div class="col-lg-12 text-center">
        		<%flash.each do |a,b| %>
        		<div class="alert alert-success"> <%= b %> </div>
        		<% end %>
          <%= yield %>
        </div>
      </div>
    </div>

<style>
      body { padding-top: 70px; }

    </style>

  </body>
</html>